# HimRate — Kamal Secrets (common to all destinations)
# Evaluated as shell script. CI sets env vars; local uses fallbacks.

KAMAL_REGISTRY_PASSWORD=${KAMAL_REGISTRY_PASSWORD:-$(gh auth token)}
RAILS_MASTER_KEY=${RAILS_MASTER_KEY:-$(cat config/master.key)}
POSTGRES_PASSWORD=$POSTGRES_PASSWORD
DATABASE_URL=$DATABASE_URL
REDIS_URL=$REDIS_URL
JWT_SECRET=$JWT_SECRET
SECRET_KEY_BASE=$SECRET_KEY_BASE
TWITCH_CLIENT_ID=$TWITCH_CLIENT_ID
TWITCH_CLIENT_SECRET=$TWITCH_CLIENT_SECRET
TWITCH_REDIRECT_URI=$TWITCH_REDIRECT_URI
