en:
  api:
    errors:
      unauthorized: "Unauthorized"
      not_found: "Not found"
      internal_error: "Internal server error"
    # TASK-090 OQ-4: MAINTENANCE_MODE middleware default message
    # (overridable via MAINTENANCE_MODE_MESSAGE env var).
    maintenance:
      message: "Scheduled maintenance. Back shortly."
  # TASK-085 FR-007 (BR-016): backend duration_text formatting per Accept-Language.
  stream_summary:
    duration_text:
      full: "%{H}h %{M}m"
      short: "%{M} min"
      multi_day: "%{D}d %{H}h"
  # T1-074 surface-audit 2026-07-21: see api.ru.yml — EN values = former hardcoded defaults verbatim.
  channels:
    errors:
      billing_not_configured: "Subscription billing is not yet configured. Contact support."
      already_tracked: "Channel is already tracked"
      not_tracked: "Channel is not tracked"
    badge_alt_v2: "HimRate: %{value} real viewers"
  tracking_requests:
    errors:
      already_requested: "Tracking request already submitted for this channel"
  clip_transcripts:
    errors:
      free_limit: "Free tier limit (10/month) reached"
      invalid_clip_id: "clip_id must be valid Twitch clip slug"
      not_found: "Transcript not found"
  erv:
    insufficient_data: "Insufficient data for ERV estimate"
